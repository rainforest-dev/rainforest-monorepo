---
import HomePage from '@components/pages/HomePage.astro';

// Default locale (English) lives at the root, unprefixed. Non-default locales are
// under /[lang] (see src/pages/[lang]/index.astro). No redirect hop.
export const prerender = true;
---

<HomePage lang="en" />
