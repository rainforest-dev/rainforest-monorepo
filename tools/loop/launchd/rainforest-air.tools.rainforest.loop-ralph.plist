<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>tools.rainforest.loop-ralph</string>
  <!-- Through the GUI session, not ralph.sh directly.
       hosts.yaml has said so since 2026-08-25: a launchd-spawned process on this
       host cannot read ~/Library/Mobile Documents. Probed with a throwaway
       LaunchAgent, `ls` on the vault's task folder returned DENIED here and 42
       files through osascript. This file said ralph.sh anyway -- so install.sh,
       and therefore the Setup page's own step, installed a runner that could not
       read the vault its two projects live in.
       run-ralph-gui.applescript carries the whole environment (LOOP_MACHINE,
       executors, LOOP_VAULT_PATH, agent config, PATH), which is why the block
       below no longer repeats it. -->
  <key>ProgramArguments</key>
  <array>
    <string>/usr/bin/osascript</string>
    <string>/Users/rainforest/.claude/loop/run-ralph-gui.applescript</string>
  </array>
  <!-- PATH only, and the system one: the loop's whole environment moves into
       run-ralph-gui.applescript, which re-enters the logged-in GUI session where
       the vault grant exists. This block needs to find osascript and nothing
       else. deriveRalphPlist emits exactly this for a host whose launchd is
       denied, and reproduces-hosts.test.ts compares the two. -->
  <key>EnvironmentVariables</key>
  <dict>
    <key>PATH</key>
    <string>/usr/bin:/bin:/usr/sbin:/sbin</string>
  </dict>
  <key>RunAtLoad</key>
  <true/>
  <key>StartInterval</key>
  <integer>1800</integer>
  <key>ThrottleInterval</key>
  <integer>60</integer>
  <key>StandardOutPath</key>
  <string>/Users/rainforest/.claude/loop/ralph.log</string>
  <key>StandardErrorPath</key>
  <string>/Users/rainforest/.claude/loop/ralph.err.log</string>
</dict>
</plist>
